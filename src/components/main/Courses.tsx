
export default function Courses() {
  return (
    <section className="flex flex-col items-center justify-center w-full max-w-7xl min-h-screen">
        {/* <h2>Courses</h2>
        {
            courses.length > 0 ?(
                courses.map((course) => (
                    <div key={course._id} className="border-2 border-gray-200 p-4 rounded-lg my-4">
                        <div>
                            <h4>{course.author.firstName} {course.author.lastName}</h4>
                        </div>
                    </div>
                ))
            ):(
                <p>No courses found.</p>
            )
        }
        <div className="flex flex-row justify-center items-center gap-4"></div>
            <button disabled={page === 1}>Prev</button>
            <span>Page {page} of {pages}</span>
            <button disabled={page === pages}>Next</button> */}
    </section>
  )
}
